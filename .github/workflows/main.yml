name: Deploy to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: ${{ php-projekt.infinityfreeapp.com }}
          username: ${{ if0_39156825 }}
          password: ${{ ygYVbxp4NJd9srZ }}
          server-dir: /htdocs/
